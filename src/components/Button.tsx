import tw from 'twin.macro';

const Button = tw.button`
  relative 
  py-2 px-4
  sm:w-auto  
  text-sm 
  rounded-full  
  focus:outline-none    
  focus:ring-2 
  focus:ring-offset-2 
`;

export default Button;
